
Oscar Brown
02/26/2018

Readings
Read chapter 3, pages 59 – 86 in the C# Step by Step book.
Discussion Questions
Answer the discussion questions in writing for chapter 3.
1. What is a method?
A method is a named sequence of statements.  Has a name and a body. body contains actual statement. 

2. What does a return statement do?
Allows a method to return information. 

3. What is an expression bodied method?
provide only a compact and cleaner way to declare a read only property. No difference in functionality from ordinary method. Simplified way of writing a method.

4. What is the scope of a variable?
The scope of a variable determines its visibility to the rest of a program. variables have been defined within methods. When a variable is declared within one of these nested scopes, it is visible only to the current scope and any scopes nested within it. Area where it is usable.

5. What is an overloadfed method?
Is what happens when two identifiers have the same name and are declared in the same scope. ex. method called pass. 2 object basketball or football. can select either but return might be different in distance.

6. How do you call a method that requirements arguments? 
put them in parentheses after method name.

7. How do you write a method, that is, specify the method definition, that requires a parameter list?
float divide(float divisor, floart divident{...}

8. How do you a parameter specify as optional when defining a method?
By providing a default value to the parameter.

9. How do you pass a argument to a method as a named parametert?

Specify the name of the parameter followed by a column and a value to use. pg 79